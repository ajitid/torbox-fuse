# Required; no default.
TORBOX_API_KEY=

# Defaults to `./torbox`.
MOUNT_PATH=./torbox

# Defaults to `./torbox-fuse.db`.
DATA_PATH=./torbox-fuse.db

# Defaults to `3h`. Valid values: `24h`, `12h`, `6h`, `3h`, `1h`, `6min`.
MOUNT_REFRESH_TIME=3h

# Defaults to `false`.
FUSE_ALLOW_OTHER=false

# Defaults to your OS cache directory plus `torbox-fuse`.
CACHE_PATH=
